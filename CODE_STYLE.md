## Themis 代码规范（Python 3）

### 1. 目标与原则
- **简单可读**：避免过度抽象，保持小模块、小函数。
- **可维护**：命名清晰、结构统一、可测试。
- **性能友好**：避免不必要的全量扫描与重计算。

### 2. 目录与模块
- 功能按“职责”拆分模块（例如：`cli` / `config` / `scanner` / `rules` / `report`）。
- 新增模块前先考虑是否可复用已有模块。

### 3. 命名规范
- 文件/模块：`snake_case.py`
- 类名：`PascalCase`
- 函数/变量：`snake_case`
- 常量：`UPPER_SNAKE_CASE`

### 4. 代码风格
- 遵循 **PEP 8**。
- 单行长度建议 **≤ 100**（必要时可超过，但尽量避免）。
- 优先使用类型注解（`typing`），公共函数必须标注输入/输出类型。

### 5. 错误处理
- 不吞异常：要么处理并记录，要么抛出清晰的错误信息。
- CLI 层统一捕获异常并输出用户可读信息。

### 6. 日志与输出
- CLI 输出与日志分离：扫描结果走标准输出，调试/诊断走标准错误。
- 默认不输出完整敏感信息（遵循脱敏策略）。

### 7. 配置与常量
- 配置优先级：命令行参数 > 平台配置文件 > 内置默认。
- 配置结构与默认值集中在配置模块统一管理。

### 8. 测试
- 关键逻辑（规则匹配、diff 解析、豁免策略、输出脱敏）必须有测试。
- 尽量覆盖边界条件与异常路径。
 - 测试框架：使用 `pytest`。

### 9. 提交要求
- 提交前确保基础测试通过。
- 避免大范围重构与无关改动混在同一次提交。

### 10. 依赖管理
- 运行依赖：`requirements.txt`（尽量保持最小）
- 开发/测试依赖：`requirements-dev.txt`
- 最低 Python 版本：`3.10+`
